<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccentPrefs</item>
    <item name="android:popupAnimationStyle">@null</item>

  </style>

  <style name="AppTheme.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
    <item name="android:popupAnimationStyle">@null</item>

  </style>

  <style name="TabWidgetTheme" parent="AppTheme">
    <item name="android:textSize">10sp</item>
    <item name="android:textAlignment">center</item>
  </style>

  <style name="animation" parent="android:Animation">

  </style>

  <style name="popup_window_animation_phone">
    <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
    <item name="android:windowExitAnimation">@anim/slide_out_left_</item>
  </style>

</resources>
